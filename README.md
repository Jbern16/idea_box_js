## Idea Box 2.0

Single Page Application!

Using client side JavaScript to handle all business and view logic.

Rails is only serving as an API.

Can create a separate app or static site to serve single page html and use Ajax calls to pull or push data to the PG database tied to the rails app.
